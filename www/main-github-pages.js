$(document).ready(function () {\n\n    // Configuração para GitHub Pages\n    const isGitHubPages = window.location.hostname.includes('github.io');\n    const API_URL = isGitHubPages ? \n        'https://jarvis-api-joao-manoel.onrender.com' : // URL da API no Render\n        'http://localhost:8000'; // URL local para desenvolvimento\n\n    console.log('🤖 Jarvis iniciando...');\n    console.log('🌐 Modo:', isGitHubPages ? 'GitHub Pages' : 'Local');\n    console.log('🔗 API URL:', API_URL);\n\n    // Inicializar apenas se não estiver no GitHub Pages\n    if (!isGitHubPages && typeof eel !== 'undefined') {\n        eel.init()()\n    }\n\n    $('.text').textillate({\n        loop: true,\n        sync: true,\n        in: {\n            effect: \"bounceIn\",\n        },\n        out: {\n            effect: \"bounceOut\",\n        },\n    });\n\n    // Siri configuration\n    var container = document.getElementById(\"siri-container\");\n    if (container) {\n        var sw = new SiriWave({\n            container: container,\n            width: container.clientWidth || 320,\n            height: 160,\n            style: \"ios9\",\n            amplitude: 1,\n            speed: 0.30,\n            autostart: true\n        });\n        window.addEventListener('resize', function() {\n            sw.setWidth(container.clientWidth || 320);\n            sw.setHeight(160);\n        });\n    }\n\n    // Siri message animation\n    $('.siri-message').textillate({\n        loop: true,\n        sync: true,\n        in: {\n            effect: \"fadeInUp\",\n            sync: true,\n        },\n        out: {\n            effect: \"fadeOutUp\",\n            sync: true,\n        },\n    });\n\n    // Função para exibir mensagens na interface\n    function displayMessage(message, isUser = false) {\n        const chatBody = document.getElementById('chat-canvas-body');\n        if (chatBody) {\n            const messageDiv = document.createElement('div');\n            messageDiv.className = `message ${isUser ? 'user-message' : 'jarvis-message'}`;\n            messageDiv.innerHTML = `\n                <div class=\"message-content\">\n                    <strong>${isUser ? 'Você' : 'Jarvis'}:</strong> ${message}\n                </div>\n                <div class=\"message-time\">${new Date().toLocaleTimeString()}</div>\n            `;\n            chatBody.appendChild(messageDiv);\n            chatBody.scrollTop = chatBody.scrollHeight;\n        }\n\n        // Atualizar mensagem principal\n        const wishMessage = document.getElementById('WishMessage');\n        if (wishMessage && !isUser) {\n            wishMessage.textContent = message;\n        }\n    }\n\n    // Função para fazer chamada à API\n    async function callAPI(message) {\n        try {\n            console.log('📤 Enviando para API:', message);\n            \n            const response = await fetch(`${API_URL}/command`, {\n                method: 'POST',\n                headers: {\n                    'Content-Type': 'application/json',\n                },\n                body: JSON.stringify({ message: message })\n            });\n\n            if (!response.ok) {\n                throw new Error(`HTTP ${response.status}: ${response.statusText}`);\n            }\n\n            const data = await response.json();\n            console.log('📥 Resposta da API:', data);\n            \n            if (data.reply) {\n                displayMessage(data.reply, false);\n                \n                // Síntese de voz (se disponível)\n                if ('speechSynthesis' in window) {\n                    const utterance = new SpeechSynthesisUtterance(data.reply);\n                    utterance.lang = 'pt-BR';\n                    utterance.rate = 0.9;\n                    speechSynthesis.speak(utterance);\n                }\n            } else {\n                displayMessage('Desculpe, não consegui processar sua solicitação.', false);\n            }\n        } catch (error) {\n            console.error('❌ Erro na API:', error);\n            \n            // Respostas de fallback\n            const fallbackResponses = {\n                'olá': 'Olá, João Manoel! Como posso ajudar você hoje?',\n                'oi': 'Oi, Sr. João Manoel! Em que posso ser útil?',\n                'como você está': 'Estou funcionando perfeitamente, João Manoel!',\n                'que horas são': `São ${new Date().toLocaleTimeString()}, Sr. João Manoel.`,\n                'que dia é hoje': `Hoje é ${new Date().toLocaleDateString()}, João Manoel.`,\n                'obrigado': 'De nada, João Manoel! Sempre à disposição.',\n                'ajuda': 'Posso ajudar com conversação, informações e muito mais, João Manoel!'\n            };\n\n            const lowerMessage = message.toLowerCase();\n            let response = null;\n\n            for (const [key, value] of Object.entries(fallbackResponses)) {\n                if (lowerMessage.includes(key)) {\n                    response = value;\n                    break;\n                }\n            }\n\n            if (!response) {\n                response = `Desculpe, João Manoel. A API está temporariamente indisponível. Tente: \"olá\", \"que horas são\" ou \"ajuda\".`;\n            }\n\n            displayMessage(response, false);\n        }\n    }\n\n    // Função para processar comandos\n    function PlayAssistant(message) {\n        if (message && message.trim() !== \"\") {\n            console.log('🎤 Comando recebido:', message);\n            \n            // Mostrar interface de conversação\n            $(\"#Oval\").attr(\"hidden\", true);\n            $(\"#SiriWave\").attr(\"hidden\", false);\n            \n            // Exibir mensagem do usuário\n            displayMessage(message, true);\n            \n            // Processar comando\n            if (isGitHubPages) {\n                // Modo GitHub Pages - usar API REST\n                callAPI(message);\n            } else {\n                // Modo local - usar Eel\n                if (typeof eel !== 'undefined' && eel.allCommands) {\n                    eel.allCommands(message);\n                } else {\n                    callAPI(message); // Fallback para API\n                }\n            }\n            \n            // Limpar input\n            $(\"#chatbox\").val(\"\");\n            $(\"#MicBtn\").attr('hidden', false);\n            $(\"#SendBtn\").attr('hidden', true);\n        }\n    }\n\n    // Mic button click event\n    $(\"#MicBtn\").click(function () {\n        if (isGitHubPages) {\n            // No GitHub Pages, mostrar mensagem sobre limitações\n            alert('🎤 Reconhecimento de voz não disponível no navegador.\\n\\n💡 Use a caixa de texto para conversar com o Jarvis!\\n\\n📱 Para funcionalidades completas, execute localmente.');\n            $(\"#chatbox\").focus();\n        } else {\n            // Modo local\n            if (typeof eel !== 'undefined' && eel.playAssistantSound) {\n                eel.playAssistantSound();\n            }\n            $(\"#Oval\").attr(\"hidden\", true);\n            $(\"#SiriWave\").attr(\"hidden\", false);\n            if (typeof eel !== 'undefined' && eel.allCommands) {\n                eel.allCommands()();\n            }\n        }\n    });\n\n    // Keyboard shortcut (Cmd/Ctrl + J)\n    function doc_keyUp(e) {\n        if (e.key === 'j' && (e.metaKey || e.ctrlKey)) {\n            $(\"#MicBtn\").click();\n        }\n    }\n    document.addEventListener('keyup', doc_keyUp, false);\n\n    // Toggle function to hide and display mic and send button\n    function ShowHideButton(message) {\n        if (message.length == 0) {\n            $(\"#MicBtn\").attr('hidden', false);\n            $(\"#SendBtn\").attr('hidden', true);\n        } else {\n            $(\"#MicBtn\").attr('hidden', true);\n            $(\"#SendBtn\").attr('hidden', false);\n        }\n    }\n\n    // Key up event handler on text box\n    $(\"#chatbox\").keyup(function () {\n        let message = $(\"#chatbox\").val();\n        ShowHideButton(message);\n    });\n\n    // Send button event handler\n    $(\"#SendBtn\").click(function () {\n        let message = $(\"#chatbox\").val();\n        PlayAssistant(message);\n    });\n\n    // Settings button\n    $(\"#SettingsBtn\").click(function () {\n        const settingsInfo = `\n🤖 JARVIS - Configurações\n\n📍 Modo: ${isGitHubPages ? 'GitHub Pages (Online)' : 'Local'}\n🔗 API: ${API_URL}\n🌐 URL: ${window.location.href}\n\n${isGitHubPages ? \n    '💡 Para funcionalidades completas (reconhecimento de voz, controle de aplicativos), execute localmente.' : \n    '✅ Modo local ativo - todas as funcionalidades disponíveis.'}\n\n📚 Documentação completa no GitHub.\n        `;\n        alert(settingsInfo);\n    });\n\n    // Enter press event handler on chat box\n    $(\"#chatbox\").keypress(function (e) {\n        if (e.which == 13) {\n            let message = $(\"#chatbox\").val();\n            PlayAssistant(message);\n        }\n    });\n\n    // Inicialização específica para GitHub Pages\n    if (isGitHubPages) {\n        // Atualizar mensagem de boas-vindas\n        setTimeout(() => {\n            displayMessage('Olá, João Manoel! Bem-vindo ao Jarvis online! Digite uma mensagem para começar.', false);\n        }, 2000);\n\n        // Adicionar estilos para mensagens\n        const style = document.createElement('style');\n        style.textContent = `\n            .message {\n                margin: 10px 0;\n                padding: 10px;\n                border-radius: 10px;\n                max-width: 80%;\n            }\n            .user-message {\n                background: #007bff;\n                color: white;\n                margin-left: auto;\n                text-align: right;\n            }\n            .jarvis-message {\n                background: #00aaff;\n                color: white;\n                margin-right: auto;\n            }\n            .message-content {\n                margin-bottom: 5px;\n            }\n            .message-time {\n                font-size: 0.8em;\n                opacity: 0.7;\n            }\n        `;\n        document.head.appendChild(style);\n\n        // Testar conectividade com a API\n        fetch(`${API_URL}/health`)\n            .then(response => response.json())\n            .then(data => {\n                console.log('✅ API conectada:', data);\n                displayMessage(`API conectada! Provedor: ${data.api_provider || 'desconhecido'}`, false);\n            })\n            .catch(error => {\n                console.warn('⚠️ API não disponível:', error);\n                displayMessage('⚠️ API temporariamente indisponível. Usando respostas básicas.', false);\n            });\n    }\n\n    // Funções expostas para compatibilidade com Eel\n    window.eel = window.eel || {};\n    window.eel.exposed_functions = window.eel.exposed_functions || {};\n    \n    window.eel.exposed_functions.receiverText = function(message) {\n        displayMessage(message, false);\n    };\n    \n    window.eel.exposed_functions.DisplayMessage = function(message) {\n        displayMessage(message, false);\n    };\n    \n    window.eel.exposed_functions.ShowHood = function() {\n        $(\"#SiriWave\").attr(\"hidden\", true);\n        $(\"#Oval\").attr(\"hidden\", false);\n    };\n\n    console.log('🚀 Jarvis inicializado com sucesso!');\n});"

// Registrar Service Worker para PWA
if ("serviceWorker" in navigator && window.location.hostname.includes("github.io")) {
    window.addEventListener("load", function() {
        navigator.serviceWorker.register("/jarvis/sw.js")
            .then(function(registration) {
                console.log("✅ Service Worker registrado:", registration.scope);
            })
            .catch(function(error) {
                console.log("❌ Falha ao registrar Service Worker:", error);
            });
    });
}
