// Este arquivo foi substituído por main-github-pages-fixed.js\n// Redirecionando para o arquivo correto...\n\nconsole.log('⚠️ Carregando arquivo corrigido...');\n\n// Carregar o arquivo correto\nif (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', loadCorrectScript);\n} else {\n    loadCorrectScript();\n}\n\nfunction loadCorrectScript() {\n    // Remover este script\n    const currentScript = document.currentScript;\n    if (currentScript) {\n        currentScript.remove();\n    }\n    \n    // Carregar o script correto\n    const script = document.createElement('script');\n    script.src = 'main-github-pages-fixed.js';\n    script.onload = function() {\n        console.log('✅ Script corrigido carregado com sucesso!');\n    };\n    script.onerror = function() {\n        console.error('❌ Erro ao carregar script corrigido');\n    };\n    \n    document.head.appendChild(script);\n}\n\n// Evitar execução do resto do código antigo\nif (typeof $ !== 'undefined') {\n    $(document).ready(function() {\n        console.log('🔄 Redirecionando para script corrigido...');\n    });\n}"